plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android { namespace "com.example.baby_monitor"; compileSdk flutter.compileSdkVersion
    defaultConfig { applicationId "com.example.baby_monitor"; minSdk flutter.minSdkVersion; targetSdk flutter.targetSdkVersion; versionCode flutterVersionCode.toInteger(); versionName flutterVersionName }
}

flutter { source "../.." }
